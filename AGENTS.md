# FluxGazer 開発エージェントガイド

この文書は、FluxGazerを変更するAIエージェントと開発者が守るプロジェクト固有の規約です。現行仕様の詳細は `doc/specification.md`、利用方法は `README.md`、公開準備状況は `doc/release_preparation.md` を参照してください。コードと文書が食い違う場合は、実装を確認したうえで両方を整合させます。

## 1. プロジェクトの目的

FluxGazerは、AIを設計・実装・検証・文書化に活用し、実用に耐える回転型モータシミュレータへ育てるプロジェクトです。現行版は開発版（Experimental）で、SPMモーターの無次元・線形・二次元静磁界FEMと、無次元トルクの参考表示を実装しています。

AIが磁界を推定しているわけではありません。解析本体は有限要素法です。実用性は目標であり、現在の結果を実機設計精度が認証された値として扱いません。

## 2. 変更前に確認するもの

1. `git status --short --branch` でユーザーの変更と未プッシュコミットを確認する。
2. 変更対象と関連テストを読む。
3. 物理・データ形式・画面仕様に触れる場合は `doc/specification.md` を読む。
4. 公開・配布・依存関係に触れる場合は `LICENSE`、`THIRD_PARTY_NOTICES.md`、`doc/release_preparation.md` を読む。

ユーザーの既存変更を上書き・破棄しません。`.idea/`、`.venv*`、`artifacts/`、キャッシュ、環境変数ファイルをコミットしません。

## 3. 起動・検証

既存のリポジトリ内仮想環境を使用します。

```powershell
.\.venv\Scripts\python.exe main.py
.\.venv\Scripts\python.exe -m flux_gazer
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe validate.py
.\.venv\Scripts\python.exe validate_torque.py
```

変更に直接関係するテストを先に実行し、完了前に全テストを実行します。磁界・トルク・GUIの変更では、数値テストだけでなく必要に応じてoffscreen描画画像も確認します。残差の小ささや見た目の滑らかさだけを物理精度の証明にしません。

## 4. コード構成

|場所|責務|
|---|---|
|`flux_gazer/geometry.py`|不変パラメータ、固定子・ロータ・磁石・コイル形状|
|`flux_gazer/definition.py`|標準形状生成、検査、モデルJSON入出力|
|`flux_gazer/excitation.py`|三相巻線、電流、dq変換|
|`flux_gazer/solver.py`|境界拘束メッシュ、材料・励磁源、一次三角形FEM|
|`flux_gazer/cache.py`|パラメータ・角度別のLRUキャッシュ|
|`flux_gazer/postprocess.py`|要素B、歯の径方向磁束密度Br|
|`flux_gazer/torque.py`|空隙マクスウェル応力による無次元トルク|
|`flux_gazer/gui.py`|非同期計算要求、シミュレーション画面、状態同期|
|`flux_gazer/editor.py`|標準形状・物性定義タブ|
|`flux_gazer/torque_plot.py`|トルク履歴、グラフ、CSV出力|
|`flux_gazer/theme.py`|ライト／ダーク配色|
|`tests/`|物理、形状、入出力、GUIの回帰検証|

GUI、ソルバー、形状、励磁、後処理、キャッシュの責務分離を維持します。計算中にUIをブロックしない構成を保ちます。Pythonのimportは相対表記を使わず、`from flux_gazer...` の絶対importにします。

## 5. 維持する物理規約

- θe = pθm。pは極対数。
- 正転は反時計回り。トルクの正も反時計回り。
- 正弦波では正の進角βが負のidを増やす。
- 六ステップ電流に正弦波のdq関係をそのまま適用しない。
- `B = (∂Az/∂y, −∂Az/∂x)`。
- 磁石の着磁は各極中心方向に一様で、N/S交互。点ごとの放射状着磁ではない。
- 「固定子電流のみ」でも磁石形状と磁石比透磁率を維持し、残留磁束密度による励磁だけを除く。
- 線形性によるPM、U−W、V−W基底の重ね合わせを維持する。ただしロータ角が変われば材料分布と行列を再構築する。
- 歯の矢印は径方向磁束密度Brであり、磁束Φや力ではない。外向き＝反発、内向き＝吸引と説明しない。
- 磁力線の外観だけで吸引・反発・トルク符号を判断しない。
- トルクは空隙のBrとBθによるマクスウェル応力から計算する。表示用Br矢印や間引き点から計算しない。
- PM単独の微小トルクをコギングトルクと断定しない。評価円、メッシュ、境界への依存性を確認する。

符号を変更する場合は相ラベルや式だけで判断せず、実際に解いた磁界、dq成分、正逆トルクで検証します。

## 6. 単位・精度の表現

現行値は無次元です。基準長L₀、基準磁束密度B₀、積厚、巻数、導体断面などを導入するまで、T、A、Nmを表示しません。SI化する場合は換算式と基準量をコード、UI、JSON、CSV、仕様書で明示します。

磁気飽和、B–H曲線、ヒステリシス、渦電流、端部効果、PWM、回路連成、逆起電力、機械運動、速度制御、負荷トルク連成は未実装です。新機能の説明で実装済みと誤解させません。

## 7. 形状・モデルファイル

形状は材料判定、境界拘束メッシュ、表示で同じポリゴンを使います。新しい寸法・極数・空隙条件では、材料領域の妥当性、非重複、軸穴、回転包絡面と歯先の最小空隙を検証します。

モデルJSONの形式識別子 `motor_sim.standard_spm` は、旧ファイルとの互換性のため維持しています。Pythonパッケージ名へ合わせて無断で変更しません。形式変更はversionを上げ、移行・後方互換性・拒否条件をテストして仕様書へ記載します。

任意CAD形状、DXF、STEPは未対応です。標準パラメータJSONを汎用CAD形式と説明しません。

## 8. GUI・履歴・出力

- 計算中は最後に完了した形状と結果を一組で表示し、古い条件の結果を新しい条件として採用しない。
- 回転アニメーションは準静的位置走査で、rpmや運動応答として表示しない。
- ライト／ダークの両方で文字、入力欄、磁力線、材料、グラフを確認する。
- Br矢印倍率は全フレーム共通で、フレーム最大値による自動正規化をしない。
- トルク履歴に異なるモデル・電流条件を混在させない。
- CSVには表示範囲外を含む保持済み計算点と、その計算条件を保存する。未計算点を補間して出力しない。

GUI操作を追加・変更したらREADMEと仕様書を更新し、主要操作のテストを追加します。

## 9. テスト方針

有効なテストは、形状の非重複、三相電流和、角度・方向・進角の符号、N/S極性、励磁単独と重ね合わせ、Br・トルクがFEM解から得られること、評価円・メッシュ・境界感度、JSON/CSV往復、非同期GUI状態を検証します。

実装をそのままなぞるだけのテストや、物理的意味のない残差閾値だけのテストを増やしません。数値比較には離散化誤差を考慮した根拠ある許容差を使います。

## 10. 文書と公開

ユーザー向け変更は `README.md`、確定した詳細仕様は `doc/specification.md`、公開状態は `doc/release_preparation.md` に反映します。新しい制限や依存関係は `THIRD_PARTY_NOTICES.md` に反映します。

コミットメッセージは、外部連携などで別の規約が必要な場合を除き、変更内容が分かる簡潔な日本語で記述します。

FluxGazer独自コードと文書はMIT Licenseです。第三者依存は各ライセンスに従います。特に現在使用するTriangle本体には商用製品への組込み制限があるため、FluxGazer全体を無条件にMITだけで再配布可能とは説明しません。商用実行ファイル配布では許諾取得またはメッシャー置換を検討します。

論理的な変更単位ごとに、必要な検証を終えてコミットします。push前に差分、追跡対象、テスト結果、公開先ブランチを確認します。リポジトリのPublic/Private変更、リリース作成、タグ付け、PyPI公開は明示的な依頼なしに行いません。

現在の公開準備では、`.idea/`をローカルに残したままGit追跡から外しています。過去履歴を書き換えない方針です。
